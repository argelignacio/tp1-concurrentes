// #[cfg(test)]

// mod tests {
//     use std::{
//         sync::{Arc, Mutex},
//         vec, collections::VecDeque,
//     };

//     use tp1::{
//         coffeemaker::coffeemaker_class::CoffeeMaker,
//         containers::{containers_class::Container, enum_containers::ContainerTypes}, mocks
//     };
//     use tp1::mocks::Dispensers;

    

//     #[test]
//     fn test_start_prepare_coffees() {
//         // let orders = vec![vec![1, 2, 3, 4], vec![3, 4, 6, 8], vec![5, 6, 1, 6]];
//         // let mut dispensers = VecDeque::new();
//         // let containers_vec: Vec<Arc<Mutex<Container>>> = create_containers();
//         // let dispensers: VecDeque<Arc<Mutex<Dispensers>>> = create_dispensers(containers_vec);
//         // let coffeemaker = CoffeeMaker::new(orders, N_DISPENSERS, dispensers);

//         // // Llamamos a la función y verificamos que no hay errores
//         // coffeemaker.start_prepare_coffees();

//         // // Verificamos que el número de cafés procesados sea igual al número total de cafés pedidos
//         // if let Ok(count_coffees_processed_guard) = coffeemaker.count_coffees_processed.lock() {
//         //     assert_eq!(
//         //         *count_coffees_processed_guard,
//         //         orders.iter().map(|order| order.len()).sum::<usize>() as i32
//         //     );
//         // }
//     }
    
// }
